package com.purplehillsbooks.pdflayout.elements.render;

import com.purplehillsbooks.pdflayout.elements.PDFDoc;

/**
 * Each element in a document is
 * {@link PDFDoc#add(com.purplehillsbooks.pdflayout.elements.Element, LayoutHint)
 * accompanied} by a hint, which gives some notes to the current layout on how
 * to layout the element.
 */
public interface LayoutHint {

}
